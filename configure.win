#!/bin/sh
# configure.win -- runs before src/ is compiled during R CMD INSTALL on Windows.
# Executed by Rtools' sh (MSYS2), not cmd.exe -- must be a POSIX shell script.
"${R_HOME}/bin/Rscript.exe" --vanilla tools/compile_abseil.R
