@echo off
REM configure.win -- runs before src/ is compiled during R CMD INSTALL on Windows.
REM Compiles all abseil .cc files into src/libabsl.a via Rtools.
"%R_HOME%\bin\Rscript.exe" --vanilla tools/compile_abseil.R
if %ERRORLEVEL% NEQ 0 exit /b 1
